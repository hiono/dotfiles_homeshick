#!/bin/bash
BUF=$(docker ps -q -f status=exited -f status=created -f status=dead -f status=paused)
[ -n "$BUF" ] && docker rm $BUF
BUF=$(docker images -q -f dangling=true)
[ -n "$BUF" ] && docker rmi $BUF
BUF=$(docker volume ls -qf dangling=true)
[ -n "$BUF" ] && docker volume rm $BUF
